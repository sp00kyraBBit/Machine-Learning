lst1 = list(range(10))
lst1.append(10) # append method add the number at the end of the list
print(lst1)

lst2 = list(range(10))
lst2.insert(0,10) # insert method take 2 parameters one index and one value. it first shifts all the value from the index onwards
#the place the new place in the index value
print(lst2)